# ClaudeClaw File Templates

Templates for all context files generated by the claudeclaw skill. Placeholders use `{{variable}}` syntax. Omit any section where the user skipped or provided no answer — never leave empty headers.

---

## SOUL.md Template

```markdown
# Soul

## Identity
- **Name**: {{name}}
- **Creature type**: {{creature_type}}
- **Vibe**: {{vibe}}

## Role
{{role}}

## Worldview
{{worldview}}

## Beliefs
{{beliefs}}

## Opinions
{{opinions}}

## Interests
{{interests}}

## Pet Peeves
{{pet_peeves}}

## Boundaries
{{boundaries}}
```

---

## STYLE.md Template

```markdown
# Communication Style

## Tone
{{tone}}

## Emoji Policy
{{emoji_policy}}

## Comment Style
{{comment_style}}

## Verbosity
{{verbosity}}

## Vocabulary
{{vocabulary}}

## Platform Differences
{{platform_differences}}
```

---

## IDENTITY.md Template (Auto-generated from SOUL interview)

```markdown
# Identity Card

> Auto-generated from SOUL.md answers. Edit SOUL.md to update.

**Name**: {{name}}
**Type**: {{creature_type}}
**Role**: {{role}}
**Vibe**: {{vibe}}
**Core trait**: {{core_trait}}
**In one sentence**: {{one_sentence_summary}}
```

---

## USER.md Template

```markdown
# Human Profile

## Basics
- **Name**: {{user_name}}
- **Pronouns**: {{pronouns}}
- **Timezone**: {{timezone}}

## Work
- **Project types**: {{project_types}}
- **Coding preferences**: {{coding_preferences}}

## Preferences
{{additional_preferences}}
```

---

## AGENTS.md Template

```markdown
# Operating Rules

## Uncertainty Handling
{{uncertainty_handling}}

## Safety Level
{{safety_level}}

## Memory Auto-Save
{{memory_auto_save}}

## Decision Framework
{{decision_framework}}

## Domain Operations
{{domain_operations}}

## Delegation
{{delegation_section}}
```

The **Domain Operations** section is populated from the domain knowledge interview — operational commands, workflows, and project-specific procedures the agent should follow.

The **Delegation** section documents when and how to use each delegation preset, spec file requirements, and the decision framework for choosing presets. Only included if delegation was set up.

Example delegation section:
```markdown
## Delegation

### Auto-Delegation Policy
- **analyze** (read-only): Invoke automatically when research or exploration would benefit from a separate context. No confirmation needed.
- **fix / fix-commit / full** (modifying): Always ask before delegating. Describe the task and suggested preset, wait for approval.

### When to Delegate
Proactively delegate when:
- A task can run in parallel with your current work
- Research would benefit from a separate, focused context
- A bug fix has a clear spec and can be handled independently
- Multiple independent changes are needed simultaneously

### Preset Selection
| Task Type | Preset | Why |
|-----------|--------|-----|
| Code review / research | analyze | Read-only, safe, auto-delegate |
| Bug fix (user reviews diff) | fix | No git, user controls commits |
| Bug fix (auto-commit) | fix-commit | TDD + auto-commit |
| Feature (full autonomy) | full | Everything except destructive ops |

### Spec File Requirements
Every delegated task needs a spec file with:
- **Problem**: What's wrong or what's needed
- **Expected behavior**: What success looks like
- **Acceptance criteria**: Testable conditions
- **Constraints**: Any limitations or rules

### How to Delegate
Run via Bash: `./claw run <preset> "<prompt>"` or `./claw run <preset> -f <spec-file>`
```

---

## TOOLS.md Template (Full mode only)

```markdown
# Tools & Environment

## Project Layout
{{project_layout}}

## SSH Hosts
{{ssh_hosts}}

## Package Managers
{{package_managers}}

## Services
{{services}}

## Delegation Tooling
{{delegation_tooling}}

## Notes
{{environment_notes}}
```

The **Project Layout** section provides a high-level map of the codebase structure — key directories, their purposes, and how they relate. Populated from context source scanning.

The **Delegation Tooling** section documents the claw script usage, preset table, examples, and key requirements. Only included if delegation was set up.

Example delegation tooling section:
```markdown
## Delegation Tooling

### Usage
```bash
./claw run <preset> <prompt>        # Inline prompt
./claw run <preset> -f <spec-file>  # Spec-driven
./claw presets                       # List presets
./claw worktrees                     # List active worktrees
./claw clean                         # Remove clean worktrees
```

### Presets
| Preset | Permissions | Use Case |
|--------|------------|----------|
| analyze | Read-only | Research, code review |
| fix | Edit + test | Bug fixes (you review diff) |
| fix-commit | Edit + test + git | Auto-commits, TDD |
| full | All except destructive | Full autonomy |

### Key Requirements
- Commit spec files to main before running (worktrees branch from HEAD)
- Child instances clear CLAUDECODE= and ANTHROPIC_API_KEY= to prevent nesting
- Each worktree gets an isolated git branch
```

---

## HEARTBEAT.md Template (Full mode only)

```markdown
# Session Heartbeat

Checks to run at the start of every session.

## Startup Checks
{{startup_checks}}

## Domain Health Checks
{{domain_health_checks}}

## Reminders
{{reminders}}
```

The **Domain Health Checks** section includes project-specific checks to run at session start — e.g., checking service status, verifying database connectivity, reviewing recent logs. Populated from the domain knowledge interview.
